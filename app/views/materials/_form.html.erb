<%= form_with(model: @material, local: true) do |form| %>
  <!-- <% if type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type.errors.count, "error") %> prohibited this type from being saved:</h2>

      <ul>
      <% type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %> -->

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
    <%= form.label :type %><br />
    <%= form.select :type, options_for_select(Type.all.map{|type|[type.name]}) %>
    <%= form.label :qty %>
    <%= form.text_field :qty %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
